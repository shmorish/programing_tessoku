#include <iostream>
#include <vector>
using namespace std;

int L[100000], R[100000];
int S[100000]; // 累積和

int main() {
    int D, N;
    cin >> D >> N;
    S[0] = 0;
    for (int i = 1; i <= N; i++) {
        cin >> L[i] >> R[i];
        
    }
    
    
}